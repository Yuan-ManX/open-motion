# OpenMotion

**AI 原生的动效设计平台。**
**动效活在真实的网页与视频里 —— 可对话修改、可组合、可被任意 AI Agent 复用。**

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](./LICENSE)
[![Status](https://img.shields.io/badge/status-积极开发中-orange)](#状态)
[![PRs Welcome](https://img.shields.io/badge/PRs-欢迎-brightgreen.svg)](#贡献)

> 动效设计，由你的 Agent 来设计。无论你是对动效好奇，还是缓动专家，OpenMotion 都让你尽情拉伸、弹跳、挤压你的想法——然后把它们交付为活的产物与可复用的 skill。

---

## 概览

OpenMotion 从第一性原理出发，重新定义动效设计。你不再面对一条只能凝视的时间轴，而是与一个动效 Agent 对话：它在动效真正运行的地方——HTML 与视频——构建、调优、交付。每一个模板在被选中的瞬间即已存活；每一个组件都可以用自然语言重塑；你打磨出的每一段动效，都能被打包成 **skill**，交给任意 AI 复用。

动效在这里是活的产物，而非静态文件。

---

## 为什么是 OpenMotion

传统动效工具围绕时间轴优化。OpenMotion 围绕**闭环**优化——意图、预览、精修、复用——这是真实产品工作真正需要的那条循环。我们把一段动效视为**可传递的设计知识单元**，它在人与 AI 之间流动。

- **动效即代码，而非工程文件。** 无导入，无导出管线。动效*本身*就是页面。
- **对话即光标。** 你描述意图，Agent 编辑曲线、节奏与图层。
- **Skill 是通货。** 一段优秀的动效成为任意 AI Agent 可调用的复用原语。

---

## 功能

### 一句话生成精致动效
通过对话与 OpenMotion 动效 Agent 协作，产出专业级动画。无论是单个重复动作，还是完整的多场景叙事——描述感觉，Agent 塑造帧。

### 完全掌控每一个关键帧
当你需要精确，时间轴归你所有。精确设定每一帧与每一属性，再邀请队友共同打磨最终动画。对话式编辑与关键帧控制并存——任意时刻自由切换。

### 广阔的创造力，精确的控制
为作品注入清晰与表现力。动画化加载状态、悬停交互、Logo 揭示、转场、滚动驱动序列——时间轴就是上限。

- **预制风格原语。** 精选风格库——Fade、Rotate、Scale、Resize、Spring、Bounce、Squash & Stretch——随时堆叠成你的构想。
- **物理缓动工坊。** 贝塞尔曲线与弹簧物理作为头等公民。Autokey 记录你的运动轨迹，再为每个关键帧配上一条它应得的曲线。
- **空间动效。** 2D 变换叠加深度——把动效与 3D 变换结合，让物体像真实世界中那样运动。
- **屏与屏之间的编排。** 每一次转场都拥有独立完整的时间轴，逐元素调节时序。

### 一次构建动效系统，处处复用
动效系统让整个团队快速在任何地方加入品牌一致的动效资产。

- **跨资产复用组件。** 把常用动作系统化——无需每次从零搭建。创建可在任意应用处生效的模式与组件。
- **动效令牌与变量。** 借助变量与设计令牌，在不同动效取值间快速切换。改一处，所有依赖动效同步更新。

### 组件级精细调优
每一段动效由相互独立的组件构成。调一个，不动其余——单独调整某个元素的缓动、延迟或变换，而周围的编排保持原样。

### 一键复用到 HTML 与视频
选中的动效直接导出为生产可用的 HTML 或渲染好的视频片段。在你浏览器里运行的那个产物，就是你交付的那个产物。

- **多格式导出。** 把任意动画帧转为 MP4、GIF、SVG、WebM，或干净的 HTML/CSS——视目标而定。

### 动效直达代码
所有动效由真实、生产可用的代码支撑。

- **代码原生输出。** 检视整条动效时间轴，直接把动画代码复制为 CSS、JSON 或 React。
- **MCP 原生 Agent 上下文。** 通过 MCP 服务器把动画代码发给你的 Agent 编码工具。所有取值——缓动、时序、变换——端到端保留。

### 任意动效导出为 Skill *（标志性原语）*
这是 OpenMotion 的定义性理念。任意动效——你的、我们的、或重新混合的——都能被打包成一个 **skill**：自包含、可被 AI 调用的动效单元。放入你 Agent 的工具箱，它便成为跨项目、跨团队、跨流程可复用的能力。

### 天生为 Agent 设计
OpenMotion 从一开始就为被 AI Agent 驱动而构建，而非仅供人类操作。平台把动效暴露为头等可编程表面——既可经自然语言、也可经结构化调用进行查询、编辑与组合。

---

## 工作原理

```
选中  →  运行  →  精修  →  交付  →  导出为 Skill
  │       │       │       │          │
  │       │       │       │          └─ 可被任意 AI Agent 复用
  │       │       │       └─ 输出为 HTML / CSS / React / MP4 / GIF / SVG / WebM
  │       │       └─ 对话 + 关键帧 + 组件级调优
  │       └─ 即刻在真实页面/视频中存活
  └─ 选取顶尖模板或描述意图
```

闭环就此合上：你交付的，正是下一个 Agent 复用的。

---

## 面向谁

- **对动效好奇的开发者**：无需时间轴即可获得生产级动效。
- **设计师**：以意图思考，让 Agent 执行细节。
- **AI Agent 构建者**：为流水线寻找可靠的动效 skill 来源。
- **团队**：按节奏交付动画化 UI、营销页与视频内容。

---

## 架构

OpenMotion 围绕四大奠基支柱组织：

| 支柱 | 职责 |
| --- | --- |
| **模板库** | 精选、持续增长的顶尖动效模板集合，开箱即活。 |
| **动效 Agent** | 对话式核心——把意图翻译为精确的参数与关键帧修改。 |
| **实时运行时** | 在真实网页与视频帧中运行动效；同一表面即交付表面。 |
| **Skill 管线** | 把任意动效打包为自包含、可被 AI 调用、可复用的单元。 |

四者合为闭环：*选中 → 运行 → 精修 → 交付 → 复用*。

---

## 快速开始

OpenMotion 正在积极开发中。首个版本发布后，你将可以：

```bash
# 克隆仓库
git clone https://github.com/Yuan-Man/open-motion.git
cd open-motion

# 安装依赖（具体指令将随首个版本给出）
# 启动 OpenMotion 工作台
```

在此之前，请关注本仓库以等候首发。

---

## 路线图

- [ ] **模板库 v1** —— 奠基级顶尖动效模板集合
- [ ] **动效 Agent** —— 跨曲线、时序与图层的对话式编辑
- [ ] **实时运行时** —— 动效在真实网页与视频帧中存活
- [ ] **组件级调优** —— 无副作用的独立组件编辑
- [ ] **代码原生导出** —— 任意时间轴输出 CSS / JSON / React
- [ ] **多格式渲染** —— MP4 / GIF / SVG / WebM 导出
- [ ] **MCP 服务器** —— Agent 可消费的动效上下文
- [ ] **Skill 管线** —— 把任意动效打包为可复用的 AI skill
- [ ] **动效系统与令牌** —— 跨资产复用组件与变量
- [ ] **空间动效** —— 3D 变换与动效结合

---

## 贡献

OpenMotion 是开源项目，欢迎贡献——模板、Agent 能力、运行时改进与 skill 创作都受益于社区输入。

1. Fork 本仓库
2. 创建你的功能分支（`git checkout -b feature/your-feature`）
3. 提交你的改动
4. 发起 Pull Request

许可条款见 [LICENSE](./LICENSE)。保持友善，保持原创，交付活的动效。

---

## 许可证

[MIT](./LICENSE) © 2026 Yuan-Man
